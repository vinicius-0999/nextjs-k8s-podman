apiVersion: v1
kind: Service
metadata:
  name: nextjs-service
  namespace: nextjs-app
  labels:
    app: nextjs-app
spec:
  selector:
    app: nextjs-app
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP